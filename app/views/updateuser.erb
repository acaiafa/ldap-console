<div class="row" style="padding-top: 100px">
  <div id="flash">
  <div class="flash info" style="color:green"><h1><%= flash[:alert] %></h1></div>
  </div>
  <h3> Update User </h3>
  <h5> This form allows you to update your users password and add SSH keys if you have them to your account. Your username is prepopulated so you do not have to worry about typing that in.<h5>
      <br><br>
      <form action='/updateuserldap' method='post' class='form-horizontal'>
          <div class='form-group'>
             <label class='col-sm-2 control-label'>Username:</label>
             <div class='col-sm-10'>
               <select name='username' class='form-control'>
                <option value="">Select...</option>
                <%= OpenLdap::User.new.list.sort.map { |x| "<option value='#{x}'>#{x}</option>" }.join %>
              </select>
            </div>
          </div>
            <div class='form-group'>
              <label class='col-sm-2 control-label'>Secondary Groups:</label>
              <div class="col-sm-10">
               <select multiple name='group_add' class='form-control'>
                <%= OpenLdap::Group.new.list.sort.map { |x| "<option value='#{x}'>#{x}</option>" }.join %>
               </select>
            </div>
          </div>
        <div class="form-group">
          <label class="col-sm-2 control-label">Update Password:</label>
          <div class="col-sm-10">
            <input type='password' name='password' class='form-control' placeholder='Password'>
          </div>
        </div>
          <div class="form-group">
        <label class="col-sm-2 control-label">SSH Keys:</label>
        <div class="col-sm-10">
          <textarea name='sshkey' rows=3 class="form-control" placeholder='SSH Keys'></textarea>
        </div>
      </div>
          <label class="col-sm-2 control-label">Suspend User:</label>
          <div class="radio-inline">
            <label>
              <input name='suspend' type='radio' value='true' />True
            </label>
          </div>
          <div class="radio-inline">
            <label>
              <input checked='checked' name='suspend' type='radio' value='false' />False
            </label>
          </div>
          <br><br>
        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
            <br><br>
            <button type='submit' value='Submit' class="btn btn-primary">Submit</button>
          </div>
        </div>
      </form>
    </div>
